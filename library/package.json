{
  "name": "@tesseract/state",
  "version": "1.0.0",
  "private": false,
  "types": "./dist/types/index.d.ts",
  "exports": {
    ".": "./src/lib/index.ts",
    "./js": "./dist/js/index.js"
  },
  "scripts": {
    "clean": "rm dist -rf && rm *.tgz -f",
    "build": "npm run clean && tsc",
    "test": "jest",
    "docs": "typedoc",
    "pack": "npm run build && npm pack",
    "release": "npm version patch && npm run build"
  },
  "devDependencies": {
    "@types/jest": "^26.0.0",
    "@types/node": "^14.0.0",
    "jest": "^26.0.0",
    "ts-jest": "^26.0.0",
    "typedoc": "^0.19.0",
    "typescript": "^4.0.0"
  },
  "dependencies": {
    "redux": "^4.0.0"
  }
}