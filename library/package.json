{
  "name": "@tesseract/state",
  "version": "1.0.5",
  "private": false,
  "main": "./js/index.js",
  "types": "./js/index.d.ts",
  "scripts": {
    "clean": "rm build -rf",
    "build": "npm run clean && tsc && npm run docs && cp package.json build && cp .npmignore build",
    "test": "jest",
    "docs": "typedoc",
    "release": "npm version patch && npm run build",
    "pack": "npm run build && cd build && npm pack",
    "deploy": "npm run build && cd build && npm publish"
  },
  "devDependencies": {
    "@types/jest": "^26.0.0",
    "@types/node": "^14.0.0",
    "@types/react-redux": "^7.1.11",
    "jest": "^26.0.0",
    "ts-jest": "^26.0.0",
    "typedoc": "^0.19.0",
    "typescript": "^4.0.0",
    "redux": "^4.0.0",
    "react": "^17.0.0",
    "react-dom": "^17.0.0",
    "react-redux": "^7.0.0"
  },
  "dependencies": {}
}
