{
  "name": "@tesseract/state",
  "version": "1.0.0",
  "private": false,
  "main": "./ts/index.ts",
  "types": "./js/index.d.ts",
  "scripts": {
    "clean": "rm build -rf && rm *.tgz -f",
    "build": "npm run clean && tsc && npm run docs && cp -r src build/ts && cp package.json build && cp .npmignore build",
    "test": "jest",
    "docs": "typedoc",
    "release": "npm version patch && npm run build",
    "pack": "npm run build && cd build && npm pack",
    "deploy": "npm run build && cd build && npm publish"
  },
  "devDependencies": {
    "@types/jest": "^26.0.0",
    "@types/node": "^14.0.0",
    "jest": "^26.0.0",
    "ts-jest": "^26.0.0",
    "typedoc": "^0.19.0",
    "typescript": "^4.0.0"
  },
  "dependencies": {
    "redux": "^4.0.0"
  }
}