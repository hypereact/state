{
  "name": "@novetica/tesseract-state",
  "version": "0.0.12",
  "private": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "rm dist -rf && npm version patch && tsc",
    "test": "jest",
    "docs": "typedoc",
    "pack": "rm *.tgz && npm pack"
  },
  "devDependencies": {
    "@types/jest": "^26.0.4",
    "@types/node": "^14.0.23",
    "jest": "^26.1.0",
    "ts-jest": "^26.1.2",
    "typedoc": "^0.19.2",
    "typescript": "^4.0.5"
  },
  "engines": {
    "node": ">=12"
  },
  "dependencies": {
    "redux": "^4.0.5"
  }
}
