{
  "name": "@tessereact/state",
  "version": "1.0.17",
  "private": false,
  "main": "./js/index.js",
  "types": "./js/index.d.ts",
  "scripts": {
    "clean": "rm build -rf",
    "build": "npm run clean && tsc && npm run docs && clear-package-json package.json -o build/package.json --fields scripts && cp .npmignore build",
    "test": "jest --runInBand",
    "docs": "typedoc",
    "release": "npm version patch && npm run build",
    "pack": "npm run build && cd build && npm pack",
    "deploy": "npm run build && cd build && npm publish"
  },
  "devDependencies": {
    "@types/jest": "^26.0.0",
    "@types/node": "^14.14.0",
    "@types/react-redux": "^7.1.0",
    "clean-publish": "^1.1.0",
    "jest": "^26.0.0",
    "react": "^17.0.0",
    "react-dom": "^17.0.0",
    "react-redux": "^7.0.0",
    "redux": "^4.0.0",
    "ts-jest": "^26.0.0",
    "typedoc": "^0.20.0",
    "typescript": "^4.1.0"
  },
  "dependencies": {}
}
